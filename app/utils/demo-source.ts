/** Shared "as of" date for the flight-hours mock snapshot.
 *  Consumed by every surface that renders flight-hours freshness so the
 *  provenance strips stay in agreement. Single source — update once. */
export const FLIGHT_HOURS_TODAY = '2026-05-31'
